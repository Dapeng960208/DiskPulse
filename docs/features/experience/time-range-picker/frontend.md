# 统一日期时间范围选择器

`frontend/src/components/form/TimeRangePicker.vue` 是全站范围查询唯一使用的日期时间选择组件。它固定使用 `YYYY-MM-DD HH:mm:ss` 传递开始和结束时间，并提供以下快捷范围：

- 1 天内
- 3 天内
- 1 周内
- 1 个月内
- 3 个月内
- 6 个月内
- 1 年内

实时监控、存储集群健康分析、存储空间监控和统一操作审计均复用该组件。维护窗口等仅填写单个开始或结束时间的表单，不属于范围查询，不使用该组件。
