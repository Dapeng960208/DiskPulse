# 前端 Mock RBAC 功能说明

## 启用

在前端开发环境设置 `VITE_USE_MOCKS=true`。登录页显示四个快捷演示账户，密码均为 `Demo@2026`。

## 行为

- 列表、详情、图表和筛选使用关联种子数据。
- 新增、编辑、删除只修改当前浏览器会话；刷新恢复默认数据。
- 非超级管理员不显示或访问系统管理；项目页面依据返回 `capabilities` 展示操作。
- 导出返回虚构 Blob；AI 返回符合客户端协议的模拟 SSE 事件。
