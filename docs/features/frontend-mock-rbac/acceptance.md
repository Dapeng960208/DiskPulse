# 前端 Mock RBAC 验收复盘

## 已验证

- Mock 网关四账户认证、项目范围拒绝、能力差异、事件更新、导出、AI 事件和资源 CRUD 的 Vitest 用例通过。
- `npm run build:test` 通过。
- 应用内浏览器发现未登录 Mock 401 导致首屏空白；已修复 Axios 空配置访问和路由守卫回跳，并记录到错误日志。

## 未验证与风险

- 应用内浏览器在该环境仍未返回可用 DOM，因此未完成四角色的逐页面视觉回归。
- 全量 Vitest 仍有两条既有路由数量断言与当前路由清单不一致；不属于本功能新增路由。
- Mock 为演示用途，不是后端授权测试或真实设备集成验证。
