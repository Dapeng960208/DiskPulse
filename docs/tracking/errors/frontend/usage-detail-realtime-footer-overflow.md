# 用户目录容量趋势延伸到页脚

## 错误内容

用户目录详情的“容量趋势”页签只声明了静态高度，未建立可收缩的 flex 高度链，也未启用实时组件的受限填充模式。较高视口下，趋势图和告警表会按自身高度撑开父内容区，进入应用页脚所在区域。

## 解决方案

为用户目录详情、标签页、容量趋势面板和实时组件依次建立 `min-height: 0` 的 flex 高度链，并启用 `RealTimePage` 的 `fillContent`。趋势图和告警表仅占用筛选和概要信息下方的剩余空间。

## 备注

- 首次出现：2026-07-20，用户目录详情“容量趋势”页签。
- 出现次数：1。
