# 开发跟踪记录：文档分类重组

- 会话：`2026-07-19-documentation-taxonomy`
- 状态：已交付
- 范围：文档分类、概览、规范和开发阅读规则重组。

本文只记录仍在进行的交付、部署前风险和本轮验证；已完成能力见[当前能力](../../../overview/product/current-capabilities.md)。

## 2026-07-19：文档分类重组

### 已完成

- 延续既有精简边界：历史计划、一次性设计、实施复盘、过期迁移记录和不再适用的工具流程不恢复为当前事实文档。
- 同一功能的设计、实施和验收描述继续收敛为唯一专题事实来源，不因目录迁移重新复制内容。
- 将文档统一为一级领域、二级主题的目录结构：概览、规范、功能专题、指南和跟踪各自独立。
- 功能专题统一为 `docs/features/<领域>/<功能>/`。AI 文档集中到 `docs/features/ai/`，存储、身份与权限、组织、前端体验使用各自领域目录。
- 规范明确拆分为前端、后端、数据库、文档和 Git 五类；新增数据库规范和开发阅读矩阵。
- 补齐产品、系统、前端和数据库概览，并保留后端架构作为后端事实入口。
- 更新 `AGENTS.md`：AI 与人工开发者按实现内容阅读通用规范、实现层规范和目标功能专题；AI 后端功能明确要求读取后端规范与 AI 功能文档。

### 验证状态

- 已校验一级/二级目录约束：功能文档均位于 `features/<领域>/<功能>/<文件>`，规范文档均位于明确的五类目录。
- 全仓已扫描旧文档路径；`docs/` 中 44 个 Markdown 文件的本地相对链接均可解析。
- `cd frontend && pnpm exec vitest run test/unit/heading-subtitle-policy.test.js --coverage.enabled=false` 通过（2 个测试）。
- `git diff --check` 通过。

### 风险

- 本轮只调整 Markdown 文档、文档索引与开发阅读约束；不修改运行时代码、配置、依赖或数据库。
- 历史计划、一次性设计和已删除功能说明仍可通过 Git 历史追溯，不作为当前事实来源。
