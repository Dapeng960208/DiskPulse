# 错误记录

无需要归档到错误库的产品运行时错误。

本会话的 RED 阶段按 TDD 预期暴露了三类缺口：`reviewed` 可保存空推荐方案、系统事件详情未返回该字段、`017` 未覆盖全部 68 个代码。这些均由本次实现和聚焦测试覆盖，不属于环境或基础设施故障。

测试库重放的首次执行没有写入数据库：将 SQLAlchemy `URL` 用 `str()` 传给 Alembic 时会隐去密码，认证立即失败。重放脚本改为仅在当前进程使用 `render_as_string(hide_password=False)` 后成功；该 URL 未写入仓库、配置或日志。随后 PostgreSQL 约束反射字段使用 `sqltext` 而非 SQLite 风格的 `definition`，读回脚本改为兼容两者并完成验证。这两项均为一次性验证脚本兼容性问题，不进入共享错误库。
