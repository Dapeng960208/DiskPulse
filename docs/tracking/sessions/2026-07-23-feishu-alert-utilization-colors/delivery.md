# 飞书告警利用率颜色交付

- 会话：`2026-07-23-feishu-alert-utilization-colors`
- 状态：已交付
- 范围：优化存储告警飞书富文本通知的信息层级、容量单位和利用率阈值颜色。

## 改动

- 飞书通知按“资源信息、容量概览”分段，移除重复的事件类型与采用口径行。
- 硬限额和软限额容量复用统一二进制显示单位，避免大容量始终显示为 GB。
- 利用率按当前有效规则的重要、严重、紧急阈值显示 🟢、🟡、🟠、🔴，边界值分别从对应阈值开始生效。
- 恢复事件继续写入告警历史并清空活动状态，但收件人为空、投递状态为 `skipped`，不会进入飞书投递队列。
- 保留恢复前等级，未改变收件人解析、异步投递、失败重试和外部 `/send_info` 协议。

## 验证

- RED：新版分段、去冗余、阈值颜色以及恢复事件不投递测试在旧实现上按预期失败。
- GREEN：`.venv\Scripts\python.exe -m pytest backend/test/test_storage_alert_rules.py -q`，39 项通过。
- 覆盖率：目标模块位于 `.coveragerc` 排除的 `backend/celery_tasks/*`，聚焦统计未采集数据，不能提供可信覆盖率百分比；见本会话错误记录。

## 未验证范围与风险

- 未连接真实飞书与通知中转服务；彩色圆点使用 Unicode 字符，仍需在部署环境确认目标飞书客户端字体的最终显示效果。
- 未执行后端全量测试；本次只运行存储告警规则与飞书投递相关的聚焦测试，且当前覆盖率策略不统计 Celery 任务模块。
