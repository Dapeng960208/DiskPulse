# 全量测试与覆盖率修复交付记录

## 范围

- 执行前端与后端全量测试，并收集覆盖率。
- 定位并修复确认的测试或覆盖率问题；每项修复遵循先验证回归、再最小实现、再回归验证的流程。

## 已完成

- 修复存储集群健康分析的异步 Tab 测试，使其等待动态组件完成加载；用完整挂载覆盖真实 Tab 行为。
- 恢复时间分析 Tab 的缓存复用：切回已打开的 Tab 不重复请求相同时间范围，时间范围变更仅刷新当前活动 Tab。
- 补齐系统事件表窄屏隐藏“来源”“事件对象”“内容”三列的响应式类。
- 同步登录用户 API mock、时间范围测试夹具和组件化后的静态源码断言，消除旧页面结构导致的测试失败。
- 已在 `main` 分支完成前端、后端全量测试与两端覆盖率收集。

## 验证

- `cd backend; ..\\.venv\\Scripts\\python.exe -m coverage erase; ..\\.venv\\Scripts\\python.exe -m coverage run -m pytest -q; ..\\.venv\\Scripts\\python.exe -m coverage report --format=total`
  - `980 passed`；后端总覆盖率 `88%`，高于仓库 `85%` 门槛。
- `cd frontend; pnpm test -- --reporter=basic`
  - `110` 个测试文件、`748` 项测试全部通过。
- `cd frontend; pnpm run test:coverage -- --reporter=dot`
  - `110` 个测试文件、`748` 项测试全部通过。
  - 总覆盖率：语句 `95.16%`、分支 `81.38%`、函数 `80.41%`、行 `95.16%`，均满足 `80%` 门槛。

## 未验证范围与风险

- 未执行连接真实后端、存储阵列或浏览器端到端环境的验收；本次验证范围为仓库自动化单元测试和覆盖率。
- Vitest 仍输出用于错误路径断言的 jsdom CORS/网络日志，以及冒烟夹具的 Vue prop 警告；测试命令均以 `0` 退出，未造成失败。
